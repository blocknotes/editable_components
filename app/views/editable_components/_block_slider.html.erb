<% if local_assigns[:block] %>
  <% local_assigns[:block].ec_blocks.each do |sb| %>
    <div class="sub-block" <%= sb.editable %>><%=
      render partial: "editable_components/block_#{sb.block_type}", locals: { block: sb }
    %></div>
  <% end %>
<% end %>
